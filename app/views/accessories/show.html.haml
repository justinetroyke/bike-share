%h1= @accessory.title

%img{:src => asset_path(@accessory.image_url)}/

%p= "Title: #{@accessory.title}"
%p= "Description: #{@accessory.description}"
%p= "Price: #{number_to_currency(@accessory.price)}"

%li
= link_to 'Add to Cart', carts_path(accessory_id: @accessory.id), method: :post
